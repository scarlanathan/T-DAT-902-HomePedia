name: homepedia
version: "1.0.0"
config-version: 2
profile: homepedia

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  homepedia:
    staging:
      +materialized: view
    core:
      +materialized: view
    marts:
      +materialized: table
